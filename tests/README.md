# Tests

Tests are organized so each chunk completes within the available execution window. Golden safety cases live in `fixtures/policy_cases.json`; adapter tests verify read-only plans, approval contracts, and argv construction.
