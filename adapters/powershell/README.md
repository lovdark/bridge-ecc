# PowerShell adapter

`adapters/powershell.py` reports PowerShell availability and builds `-NoProfile -NonInteractive -Command` argv lists without shell interpolation. Policy analysis remains in the Python core.
